{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "hypercorn src.main:app --bind [::]:$PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "internal": true
  }
}
